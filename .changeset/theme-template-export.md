---
'overlook': minor
---

Settings → Appearance can now export a theme template: a complete `.overlook-theme.json` capturing every effective design token with a one-line description per token, ready to hand to a person or an LLM and re-import as-is.

---
'overlook': patch
---

Make double-click Fill in full view actually cover the viewport: the image now scales by the larger viewport-to-image ratio, so it reaches all four edges and overflows only the single longer axis, instead of leaving bars beside square, near-square, and moderately proportioned photos.

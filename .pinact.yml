# yaml-language-server: $schema=https://raw.githubusercontent.com/suzuki-shunsuke/pinact/main/json-schema/pinact.json
version: 3
# ci-policy is the authorization boundary (see #1038, #1024). Never bump via automation.
ignore_actions:
  - name: qwts/playbook-engineering/.github/actions/ci-policy
    ref: ".*"
